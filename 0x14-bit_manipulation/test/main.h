#ifndef MAIN_H_
#define MAIN_H_

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

char _putchar(char *);
unsigned int binary_to_uint(const char *b);
void print_binary(unsigned long int n);

#endif
